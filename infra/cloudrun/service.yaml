apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: chatrag
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "2"
        autoscaling.knative.dev/maxScale: "4"
        run.googleapis.com/cpu-throttling: "false"
    spec:
      containerConcurrency: 80
      timeoutSeconds: 300
      containers:
        - image: gcr.io/PROJECT_ID/chatrag:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 16Gi
              cpu: "8"
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "8080"
            - name: DATABASE_URL
              value: "REPLACE_WITH_CLOUD_SQL_URL"
            # Chroma runs in-process (local PersistentClient) for lowest latency
            - name: CHROMA_MODE
              value: local
            - name: ANONYMIZED_TELEMETRY
              value: "False"
            - name: OPENAI_API_KEY
              value: "REPLACE_WITH_OPENAI_KEY"
            - name: STORAGE_PROVIDER
              value: disk
            - name: WORKER_MODE
              value: local
            - name: WORKER_JOB_NAME
              value: chatrag-worker
            - name: WORKER_REGION
              value: europe-west1
            - name: FRONTEND_DIST_PATH
              value: /app/frontend/dist
            - name: PYTHON_BIN
              value: /app/python/.venv/bin/python3
            - name: PYTHON_PROJECT_ROOT
              value: /app/python
            - name: PYTHON_SERVER_URL
              value: "http://localhost:8321"
            # URL of the chatrag-indexer Cloud Run Service (for PDF offloading)
            - name: INDEXER_URL
              value: "REPLACE_WITH_INDEXER_SERVICE_URL"
            - name: INDEXER_SECRET
              value: "REPLACE_WITH_SHARED_SECRET"
