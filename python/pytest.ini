[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = src
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning:importlib._bootstrap
markers =
    slow: mark test as slow-running (uses real files, API calls, or heavy computation)
