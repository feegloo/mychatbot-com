{
  "name": "chatrag-tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tauri 2 mobile and desktop wrapper for ChatRAG",
  "scripts": {
    "frontend:dev": "cd ../frontend && npm run dev -- --host 0.0.0.0 --port 5173",
    "frontend:build": "cd ../frontend && npm run build",
    "frontend:check": "cd ../frontend && npm run check",
    "frontend:lint": "cd ../frontend && npm run lint",
    "check": "npm run frontend:check",
    "lint": "npm run frontend:lint",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev",
    "ios:build": "tauri ios build",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build",
    "publish:appstore": "bash scripts/publish-appstore.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.1"
  }
}
